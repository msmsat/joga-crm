# Эпик 6 — Velora AI (self-hosted LLM с инструментами) + финальная полировка

> Настоящий ИИ-ассистент на собственной модели: видит расписание и статистику с учётом роли, переводит по страницам, меняет данные через подтверждение. Плюс добивка Настроек, тёмная тема, i18n и финальный аудит.
> Источник: [`docs/ROADMAP.md`](ROADMAP.md), Эпик 6. Модель — **self-hosted** (сервер с GPU 24 ГБ), без зависимости от внешних API.

## Выбор модели и сервинга (решение зафиксировано)

Критерии: русский язык, нативный tool calling, помещается в 24 ГБ VRAM, коммерческая лицензия, живое комьюнити для дообучения.

- **Основная: Qwen3-14B** (Apache 2.0) в 4-бит квантовании — лучший баланс русского языка и function calling для 24 ГБ; линейка Qwen3/3.5 — топ мультиязычных open-моделей 2026 года, Qwen3-14B называют оптимумом «производительность/эффективность» для русского.
- **Альтернатива «от Google»: Gemma 3 27B** в Q4 — нативный function calling (модель обучена выдавать структурные tool-вызовы); с прицелом на Gemma 4, когда стабилизируется в квантованном виде.
- **Русскоязычные тюны для этапа дообучения:** T-pro/T-lite (Т-Банк, Qwen-based), Saiga, Vikhr — если «натуральность» русского окажется важнее.
- **Сервинг:** старт — **Ollama** (OpenAI-совместимый API из коробки); прод при нагрузке — **vLLM** (тот же контракт, бэкенд не меняется).
- **Дообучение — НЕ начинать с него.** Системный промпт + инструменты закрывают ~90 % задач. LoRA (Unsloth) на накопленных реальных диалогах CRM — отдельный шаг после MVP.

## Что уже есть в коде (точка старта)

- **Модели БД готовы** (`back/models/ai.py`): `StudioAISettings` (model/language/system_prompt + полные конфиги TG/IG-агентов со статистикой handled_count/avg_rating), `AIChatSession`, `AIChatMessage`. Миграции не нужны.
- **Бэкенд — чистое поле:** `back/routers/ai/chat.py` и `back/schemas/ai/*` — заглушки, ноль эндпоинтов.
- **Контракт фронта готов:** `front/src/api/ai/ai.api.ts` — 7 методов (`/ai/sessions` CRUD, messages, `/ai/settings`). Гэп: тип `AISettings` не содержит `*_max_length` и полей статистики из модели.
- **Четыре независимых мок-чата на фронте:** страница `AI/` (`useAIChat.ts` + `MOCK_AI_RESPONSES`), глобальный дровер `components/AIDrawer/` (`useDrawerChat.ts`), AI-строка в шапке (`DashboardLayout.tsx:73-82` — «Подключите сюда реальный вызов»), чат Журнала (`Journal/components/AiChat.tsx:33` — «демо-режим»). Общий только `AIDrawerContext` (open/close).
- **`MODEL_OPTIONS` в `AI/constants.ts`** — чужие API-модели (gpt-4o, claude, gemini) — заменить на свои self-hosted.
- **Настройки:** роутеры `back/routers/settings/*` — заглушки; вкладки фронта — локальные моки; `settingsApi` уже типизирован (general, working-hours, security, integrations). Логотип в `GeneralTab.tsx` — base64 в стейт; паттерн загрузки готов в `back/routers/studio/media.py` (**`POST /upload-logo` сейчас без auth — дыра**). Модели `UserSession`, `ApiToken`, `StudioIntegration`, `StudioBackupSettings`, `StudioWorkingHours` — готовы.
- **Тёмной темы не существует** (нет data-theme/персиста; `AppearanceTab` — косметика). CSS-переменные есть в `App.css :root`.
- **i18n:** i18next настроен, но дефолт `en` (баг для RU-продукта), язык не персистится; переведены только Staff/Profile/Catalog.

## Жёсткие правила эпика (MVP)

1. **Никакой привязки к вендору:** бэкенд говорит по OpenAI-совместимому протоколу; смена модели/сервера = `LLM_BASE_URL`/`LLM_MODEL` в .env (Ollama ↔ vLLM ↔ что угодно).
2. **Ассистент = агентный цикл с инструментами**, и каждый инструмент выполняется **с ролью и студией текущего пользователя** (get_studio_context/require_role из Эпика 1): тренер через ИИ физически не достанет чужие данные.
3. **Изменяющие действия — только через подтверждение:** mutating-инструменты не исполняются внутри цикла; ассистент возвращает «предложение действия», человек подтверждает кнопкой — галлюцинация модели не испортит расписание.
4. **Одна серверная история чатов для всех поверхностей** (раздел AI, дровер, AI-строка, Журнал — ТЗ 12.4): четыре мок-движка заменяются одним хуком.
5. **Стриминг SSE** — токены + статусы «вызываю инструмент…»: локальная модель отвечает секундами, без стрима ассистент ощущается зависшим.

**Обозначения сложности:** 🟢 простая · 🟡 средняя · 🔴 сложная, делать внимательно.

---

## Бэклог

| № | Фаза | Задача | Кто | Сложность | Время | Простыми словами | Зачем | Файлы | Что сделать | Готово когда |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | 1. Модель и сервинг | Поднять self-hosted LLM (Qwen3-14B + Ollama) | Инфра | 🔴 Сложная | 1-2ч | Развернуть свою модель на сервере с GPU 24 ГБ и убедиться что она держит русский и вызовы инструментов | Независимость от внешних API — базовое требование; всё остальное строится на этом | сервер (Ollama); back/.env.example (LLM_BASE_URL; LLM_MODEL); docs/STATUS.md | Установить Ollama; ollama pull qwen3:14b (Q4); дымовой тест: русский диалог + tool calling через OpenAI-совместимый /v1/chat/completions; замерить скорость токенов; запасной вариант — gemma3:27b-q4; зафиксировать выбор и замеры в docs/STATUS.md; vLLM — прод-опция позже без смены кода | Модель отвечает по-русски; корректно вызывает тестовый инструмент; endpoint доступен бэкенду по LLM_BASE_URL |
| 2 | 1. Модель и сервинг | LLM-клиент бэкенда | Бэкенд | 🟡 Средняя | 1-2ч | Один модуль общения с моделью: чат; инструменты; стриминг — без привязки к конкретному серверу | Смена модели или переезд Ollama→vLLM = одна переменная .env; код не трогаем | новый back/services/llm.py; back/.env.example | chat(messages; tools; stream) по OpenAI-совместимому протоколу через aiohttp (уже в requirements); base_url и model из .env (паттерн back/security.py); таймауты и понятная ошибка если LLM-сервер лежит | Из консоли бэкенда уходит запрос и приходит стрим-ответ; при выключенном Ollama — внятная ошибка а не зависание |
| 3 | 2. Ассистент-агент | Схемы AI; CRUD сессий и настроек | Бэкенд | 🟡 Средняя | 1-2ч | Заполнить схемы; сделать сессии чата и настройки ассистента (модель; язык; промпт; конфиги TG/IG-агентов) | Контракт aiApi уже ждёт 7 эндпоинтов; настройки — база системного промпта | back/schemas/ai/chat.py; settings.py; back/routers/ai/chat.py; новый back/routers/ai/settings.py; front/src/api/ai/ai.types.ts | GET/POST/DELETE /ai/sessions; GET /ai/sessions/{id}/messages; GET/PATCH /ai/settings (get-or-create; дефолт model — свой self-hosted вместо gpt-4o); закрыть гэп типов: добавить tg_max_length/ig_max_length и статистику handled_count/avg_rating во фронт-тип AISettings; скоуп по studio_id; сессии — свои для каждого пользователя | Сессия создаётся/удаляется; настройки переживают перезапуск; поля типов совпадают со схемами |
| 4 | 2. Ассистент-агент | Реестр инструментов с ролевым скоупом | Бэкенд | 🔴 Сложная | 1-2ч | Набор инструментов ассистента: расписание; клиенты; статистика; настройки; навигация — каждый работает от имени и роли пользователя | «Взаимодействует с графиком; знает роль; помогает со статистикой» — сердце фичи; безопасность: тренер через ИИ не достанет чужое | новый back/services/ai_tools.py | Описать инструменты в формате OpenAI tools: get_schedule(date_from; date_to); create_lesson(...); get_clients(search); get_client(id); book_client(...); get_stats(period) → агрегаты Эпика 3; get_settings/update_setting; navigate(page) — enum страниц CRM; каждый инструмент вызывает ту же логику что обычные эндпоинты с get_studio_context и require_role текущего пользователя; у каждого флаг mutating: true/false | Вызов get_schedule тренером возвращает только его занятия; get_stats отдаёт цифры /analytics/summary; mutating-флаги проставлены |
| 5 | 2. Ассистент-агент | Агентный цикл: LLM ↔ инструменты | Бэкенд | 🔴 Сложная | 1-2ч | Обработка сообщения: модель думает; зовёт инструменты; получает результаты; отвечает по-русски | Превращает чат-болталку в ассистента который реально смотрит в данные студии | back/routers/ai/chat.py | POST /ai/sessions/{id}/messages: системный промпт = роль + имя студии + сегодняшняя дата + текущая страница (передаёт фронт) + system_prompt из StudioAISettings; цикл: ответ модели → если tool_calls — исполнить safe-инструменты → результаты обратно в модель; лимит 6 итераций; mutating-инструменты НЕ исполнять (задача 6); сохранить user+assistant сообщения в AIChatMessage; обновить preview сессии | Вопрос «сколько занятий у меня завтра» отвечает реальными данными из БД с учётом роли; история сохраняется |
| 6 | 2. Ассистент-агент | Подтверждение изменяющих действий | Бэкенд | 🔴 Сложная | 1-2ч | ИИ не меняет данные молча: предлагает действие; человек подтверждает кнопкой; только тогда исполняется | «Менять что-то» без этого опасно: галлюцинация модели не должна портить расписание студии | back/routers/ai/chat.py; back/services/ai_tools.py | Когда модель зовёт mutating-инструмент — не исполнять; вернуть в ответе структурный блок action_proposal {tool; args; человекочитаемое описание}; POST /ai/actions/execute (tool; args) — исполняет с теми же ролевыми проверками после подтверждения на фронте; проверять что tool из реестра а args валидны по его схеме | «Создай занятие завтра в 10» возвращает предложение а не создаёт; execute создаёт; тренер не может выполнить чужое действие |
| 7 | 2. Ассистент-агент | Стриминг ответов (SSE) | Бэкенд | 🟡 Средняя | 1-2ч | Ответ прилетает токен за токеном плюс статусы «смотрю расписание...» | Локальная модель отвечает секундами — без стрима ассистент ощущается зависшим (боль «долгие ответы») | back/routers/ai/chat.py | StreamingResponse (text/event-stream) поверх задачи 5: события token; tool_status (имя инструмента); action_proposal; done; полное сообщение в БД по завершении | В curl виден постепенный поток событий; между вызовами инструментов приходят статусы |
| 8 | 3. AI-фронтенд | Раздел Velora AI на реальный API | Фронтенд | 🔴 Сложная | 1-2ч | Страница AI: живой стрим-чат; история сессий с сервера; настройки ассистента и TG/IG-агентов сохраняются | Главная витрина ИИ перестаёт отвечать четырьмя заготовленными фразами | front/src/pages/dashboard/AI/hooks/useAIChat.ts; useAIAgent.ts; AI/constants.ts; components/LeftPanel.tsx; modals/AgentSetupModal.tsx; front/src/api/ai/ai.api.ts | useAIChat → aiApi + чтение SSE (fetch + ReadableStream); история и сессии с сервера; выпилить MOCK_SESSIONS/MOCK_AI_RESPONSES; MODEL_OPTIONS заменить на свои self-hosted модели (qwen3-14b; gemma3-27b); useAIAgent.saveConfig → PATCH /ai/settings; статистика агентов из handled_count/avg_rating | Вопрос по студии получает стрим-ответ с реальными цифрами; F5 сохраняет историю; настройки агентов переживают перезапуск |
| 9 | 3. AI-фронтенд | Единый чат-движок: дровер; AI-строка; Журнал | Фронтенд | 🔴 Сложная | 1-2ч | Все четыре точки входа ИИ работают через одну серверную историю вместо четырёх независимых моков | ROADMAP/ТЗ 12.4 требуют единую историю; сейчас четыре разных заглушки | новый front/src/hooks/useAssistant.ts (общий); components/AIDrawer/hooks/useDrawerChat.ts; AIDrawer/constants.ts; layouts/DashboardLayout.tsx (handleAskAi:73-82); pages/dashboard/Journal/components/AiChat.tsx | Вынести SSE-чат-логику из задачи 8 в общий хук useAssistant(sessionId); дровер и Журнал — те же /ai/sessions (сессии общие с разделом AI); AI-строка в шапке: handleAskAi → быстрый вопрос в новую/последнюю сессию с ответом в выпадашке; передавать текущий route как контекст страницы; выпилить MOCK_DRAWER_* и хардкод-фразы | Начатый в дровере диалог виден в истории раздела AI; AI-строка отвечает реальными данными; Журнальный чат знает про текущий день расписания |
| 10 | 3. AI-фронтенд | Действия ассистента в UI: навигация и подтверждения | Фронтенд | 🔴 Сложная | 1-2ч | «Открой отчёты» реально переводит на страницу; «создай занятие» рисует карточку подтверждения с кнопкой | Замыкает главные обещания фичи: переносить по страницам и менять данные безопасно | front/src/hooks/useAssistant.ts; AIDrawer/components/ChatView.tsx; AI/components/MessageBubble.tsx (карточка действия) | Обрабатывать событие navigate → useNavigate() на маршрут из enum; action_proposal → карточка в чате (описание; Подтвердить/Отмена); подтверждение → POST /ai/actions/execute → тост об успехе и инвалидация данных страницы; отказ — сообщение в чат | «Открой финансы» переходит в Финансы; «запиши Анну на завтра 10:00» после подтверждения создаёт запись видимую в Журнале |
| 11 | 4. Настройки | Общие; часы работы; логотип | Бэкенд + Фронтенд | 🔴 Сложная | 1-2ч | Вкладки Основные и Рабочие часы сохраняются на сервер; логотип реально загружается файлом | Первое что видит владелец в Настройках; заодно закрываем дыру безопасности на загрузке | back/routers/settings/general.py (заглушка); front/src/pages/dashboard/Settings/components/tabs/GeneralTab.tsx; HoursTab.tsx; back/routers/studio/media.py | GET/PATCH /settings/general (Studio поля); GET/PUT /settings/working-hours (StudioWorkingHours); на POST /upload-logo НАВЕСИТЬ auth (сейчас открыт всем — дыра) и сохранять URL в студию; GeneralTab: base64-логику заменить загрузкой файла + URL; подключить обе вкладки к settingsApi (контракт уже типизирован) | Название/контакты/часы переживают F5; логотип виден после перезагрузки; аноним получает 401 на upload-logo |
| 12 | 4. Настройки | Безопасность: сессии и API-токены | Бэкенд + Фронтенд | 🔴 Сложная | 1-2ч | Список реальных активных сессий с выходом на других устройствах; выпуск и отзыв API-токенов | ROADMAP Эпика 6; сейчас вкладка Безопасность — театр | back/routers/settings/security.py; back/routers/auth/login.py (создание сессии); front/src/pages/dashboard/Settings/components/tabs/SecurityTab.tsx; hooks/useSecurity.ts | При логине создавать UserSession (device/platform из User-Agent; token_hash); GET /settings/security/sessions; DELETE /{id} (отзыв → токен невалиден); API-токены: POST (показать один раз; хранить hash + prefix); DELETE (revoked_at); подключить SecurityTab к settingsApi; 2FA — НЕ делаем (вне рамок) | Вход с двух браузеров даёт две сессии; отзыв разлогинивает второй; токен показывается один раз и отзывается |
| 13 | 4. Настройки | Интеграции и экспорт данных | Бэкенд + Фронтенд | 🟡 Средняя | 1-2ч | Карточки интеграций сохраняют конфигурацию и статус; кнопка экспорта отдаёт архив данных студии | ROADMAP: сохранение конфигурации без реальных коннекторов; экспорт — доверие к продукту; долг Эпика 5 — переименовать ЮKassa | back/routers/settings/integrations.py; новый back/routers/settings/data.py; front/.../tabs/IntegrationsTab.tsx (строка 30 — карточка ЮKassa); DataTab.tsx | GET /settings/integrations; PATCH /settings/integrations/{type} (StudioIntegration upsert; config JSON; is_connected); реальные коннекторы НЕ пишем — только конфиг и статус; карточку «Яндекс.Касса (ЮKassa)» переименовать в Fondy; GET /settings/data/export → JSON-архив (клиенты; занятия; операции) через StreamingResponse; StudioBackupSettings GET/PATCH | Конфиг интеграции переживает перезапуск; экспорт скачивает файл с реальными данными студии; карточка называется Fondy |
| 14 | 5. Полировка UX | Тёмная тема по-настоящему | Фронтенд | 🔴 Сложная | 1-2ч | Переключатель темы реально красит интерфейс в тёмное и запоминает выбор | Тёмная тема обещана ТЗ и дизайн-системой (#121212/#1E1E1E); сейчас её не существует вообще | front/src/App.css; новый front/src/contexts/ThemeContext.tsx; front/src/App.tsx; Settings/components/tabs/AppearanceTab.tsx | Ввести data-theme="dark" на html; в App.css добавить тёмный набор существующих CSS-переменных по дизайн-системе CLAUDE.md §6 (фон #121212; карточки #1E1E1E); ThemeContext: light/dark/auto (prefers-color-scheme); персист в localStorage; AppearanceTab переключает реально; пройтись по страницам — места с захардкоженными цветами перевести на переменные точечно | Переключение мгновенно красит весь интерфейс; выбор переживает F5; auto следует системной теме |
| 15 | 5. Полировка UX | i18n: дефолт ru; персист; докрытие | Фронтенд | 🟡 Средняя | 1-2ч | Продукт стартует на русском; выбор языка запоминается; ключевые непереведённые экраны получают словари | Сейчас дефолт en (баг для RU-продукта) и язык сбрасывается; ТЗ обещает мультиязычность | front/src/i18n.ts; front/src/locales/**; Settings/* и AI-поверхности (хардкод) | lng: localStorage либо 'ru' (fallback ru); переключатель языка в Настройках пишет в localStorage и i18n.changeLanguage; добавить namespace settings и ai — перевести Settings и AI-поверхности (остальные страницы — по мере; зафиксировать список долга в docs/STATUS.md) | Первый запуск — на русском; смена языка переживает F5; вкладки Настроек переведены в en |
| 16 | 5. Полировка UX | Финальный аудит фронт ↔ бэк по ТЗ | Бэкенд + Фронтенд | 🟡 Средняя | 1-2ч | Пройти по docs/TZ.md раздел за разделом и зафиксировать что работает; что на моках; что вне MVP | Финальная точка roadmap: честная картина продукта перед релизом | docs/STATUS.md; docs/TZ.md (только чтение) | По каждому разделу ТЗ отметить: работает / мок / вне MVP; проверить сквозные задачи ROADMAP (пагинация; 403; лента; бейджи; типы); собрать список известных долгов (i18n-остаток; автоответчики TG/IG; 2FA; авто-списание) в docs/STATUS.md; npm run build && npm run lint — чисто | Обновлённый STATUS.md отражает реальность; список долгов явный; сборка зелёная |

---

## Порядок работы (что за чем)

Фазы строго **1 → 2 → 3**, затем 4–5 (могут идти параллельно с фазой 3; задача 16 — строго последняя). Внутри фазы 2: **3 → 4 → 5 → 6 → 7**. Внутри фазы 3: **8 → 9 → 10**. Задачи 4–6 требуют работающих Эпиков 1–3 (ролевой скоуп, занятия, агрегаты аналитики).

## Что специально НЕ входит в Эпик 6

- **Реальные автоответчики TG/Instagram** — каналы физически не подключены (бота нет); конфигурация и статистика готовы, включение — после появления каналов.
- **Дообучение модели** — начинаем с промпта+инструментов; LoRA (Unsloth) на накопленных реальных диалогах — отдельный пост-MVP шаг.
- **2FA** — сессии и токены делаем, двухфакторку — post-MVP.
- **RAG по документам студии, выбор из нескольких моделей на лету, голосовой ввод** — post-MVP.
- **Полное i18n-покрытие всех страниц** — докрываем Settings + AI, остальное списком долга в STATUS.md.

## Как проверить (Verification)

1. **Модель:** дымовой тест Ollama — русский ответ + вызов тестового инструмента; замер токенов/сек на 24 ГБ.
2. **Ролевой скоуп ИИ:** под тренером спросить «покажи всё расписание студии» — ассистент отдаёт только его занятия; под владельцем — все.
3. **Цикл с данными:** «какая выручка за месяц?» → цифра совпадает с Дашбордом; «открой отчёты» → переход на страницу; «создай занятие завтра 10:00» → карточка подтверждения → после клика занятие видно в Журнале.
4. **Единая история:** диалог, начатый в дровере, виден в истории раздела AI после F5.
5. **Настройки:** логотип/часы/сессии/токены/интеграции переживают перезапуск; аноним на /upload-logo → 401; тёмная тема и язык переживают F5.
6. `npm run build && npm run lint` — чисто; docs/STATUS.md обновлён и отражает реальность.
