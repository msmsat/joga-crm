# Эпик 3 — Финансы, Отчёты и Дашборд (реальные данные и аналитика)

> Три разных среза одних и тех же реальных данных о клиентах, занятиях и деньгах — поверх общего агрегирующего слоя.
> Источник: [`docs/ROADMAP.md`](ROADMAP.md), Эпик 3.

## Что уже есть в коде (точка старта)

- **Модели БД полностью готовы, миграции не нужны:** `back/models/finances.py` (Account, Operation, Counterparty, FinDocument, FinancialGoal, SalaryPayment), `back/models/reports.py` (StudioTask — виджет задач Дашборда, ActivityLog — лента событий), `back/models/analytics.py` (DailyMetricSnapshot — в этом эпике НЕ используем). Деньги хранятся как Integer.
- **Роутеры и схемы — пустые заглушки:** `back/routers/finances/*` и `back/routers/analytics/*` — только `router = APIRouter()`; `back/schemas/finances/*` и `back/schemas/analytics/*` — комментарии-заготовки. Префиксы `/finances` и `/analytics` уже зарегистрированы в `main.py`.
- **Фронтовый контракт уже задан:** `front/src/api/finances/finances.api.ts` + `finances.types.ts` и `front/src/api/analytics/analytics.api.ts` + `analytics.types.ts` существуют и полностью типизированы, но не подключены к UI. Бэкенд пишем ровно под эти типы.
- **UI на 100% моках:** `Finances/constants.ts` (все 9 вкладок), `Reports/*` (6 вкладок, множители в `useReportData.ts`), `Overview/constants.ts` (метрики, график, лента, задачи). Ноль API-вызовов.
- **Экспорт уже работает механически:** `Reports/hooks/useExport.ts` — CSV через Blob + BOM, PDF = `window.print()`. Нужны только реальные данные.
- **Эталон для копирования паттернов** — `back/routers/clients/profiles.py`.
- **Из Эпика 1:** обёртка `Page` (`back/schemas/common.py`), `get_studio_context` и `require_role` (`back/dependencies.py`). **Из Эпика 2:** занятия и записи (нужны для зарплат, посещаемости, «записей на сегодня»). Если они не готовы — сначала закрыть их.

## Жёсткие правила эпика (MVP)

1. **Никаких OLAP и снапшотов.** Все агрегаты — живые SQLAlchemy-запросы (SUM/COUNT/GROUP BY/date_trunc) по `operations`, `lessons`, `reservations`, `clients`. `DailyMetricSnapshot` не трогаем — это оптимизация после MVP.
2. **Операции — единственный источник денежных фактов.** Выручка и расходы везде считаются из таблицы `operations`; зарплатная выплата сама создаёт расходную операцию.
3. **Дашборд не имеет своих эндпоинтов** — питается из `/analytics/summary`, `/series`, `/trainers`, `/services`, `/activity`, `/tasks`. Ноль дублирования логики.
4. **Список операций — строго `Page[T]`** (контракт Эпика 1: items/total/offset/limit) с серверными фильтрами.
5. **Производные поля не храним:** `Account.daily_change`, `Counterparty.balance/deals_count`, `FinancialGoal.current_amount` (auto-режим) считаются на лету из операций.
6. **Доступ:** всё `/finances/*` — `require_role("owner")`; `/analytics/*` — все роли (Дашборд по ТЗ доступен всем), страница Отчётов закрыта owner-ом на фронте.

**Обозначения сложности:** 🟢 простая · 🟡 средняя · 🔴 сложная, делать внимательно.

---

## Бэклог (строго бэкенд → фронтенд)

| № | Фаза | Задача | Кто | Сложность | Время | Простыми словами | Зачем | Файлы | Что сделать | Готово когда |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | 1. Базовые CRUD Финансов | Схемы Финансов и Аналитики | Бэкенд | 🟡 Средняя | 1-2ч | Описать в каком виде счета; операции; контрагенты; цели; зарплаты; задачи и события ходят между сервером и фронтом | Контракт данных — фундамент всех задач эпика; фронт-типы уже существуют и диктуют поля | back/schemas/finances/accounts.py; operations.py; salary.py; новый goals.py; back/schemas/analytics/reports.py; tasks.py; back/schemas/finances/__init__.py и analytics/__init__.py | Заполнить заготовки классами AccountRead/Create/Update; OperationRead/Create; CounterpartyRead/Create/Update; FinDocumentRead/Create; GoalRead/Create/Update; SalaryRead; StudioTaskRead/Create/Update; ActivityLogRead — наследовать BaseSchema из back/schemas/_base.py; поля строго 1:1 с front/src/api/finances/finances.types.ts и analytics/analytics.types.ts | Все схемы импортируются; поля совпадают с фронт-типами; суммы Integer как в моделях |
| 2 | 1. Базовые CRUD Финансов | Счета: список; создание; правка | Бэкенд | 🟡 Средняя | 1-2ч | Сервер отдаёт счета студии (касса; расчётный; эквайринг) и позволяет добавить свой счёт | Вкладка «Счета и кассы» сейчас рисует выдуманные балансы | back/routers/finances/accounts.py | GET /finances/accounts; POST; PATCH /{id}; DELETE /{id} с require_role(owner) и скоупом studio_id через get_studio_context; daily_change НЕ хранить — считать на лету как сумму операций счёта за сегодня | Через Swagger счёт создаётся; список отдаёт балансы; чужой studio_id недоступен; daily_change меняется после добавления операции за сегодня |
| 3 | 1. Базовые CRUD Финансов | Операции: пагинированный список с фильтрами | Бэкенд | 🔴 Сложная | 1-2ч | Отдавать доходы и расходы порциями с фильтрами по типу; категории; счёту; датам и поиском | Главная таблица финансов; на тысячах операций без пагинации фронт умрёт | back/routers/finances/operations.py | GET /finances/operations в обёртке Page[OperationRead] (контракт Эпика 1 из back/schemas/common.py); фильтры type; category; account_id; client_id; date_from; date_to; search по title; offset/limit; сортировка по op_date DESC | Ответ содержит items и total; фильтр по типу и датам сужает выборку; порция 20 штук при total = реальному числу в базе |
| 4 | 1. Базовые CRUD Финансов | Операции: создание и баланс счёта | Бэкенд | 🟡 Средняя | 1-2ч | Создание дохода/расхода сразу двигает баланс привязанного счёта | Единый источник денежных фактов; на нём стоит вся аналитика эпика | back/routers/finances/operations.py | POST /finances/operations по OperationCreate; проставить studio_id; проверить принадлежность account_id/client_id/counterparty_id студии; при type=in прибавить amount к Account.balance; при type=out вычесть; DELETE /{id} с откатом баланса | Создал доход 1000 — баланс счёта вырос на 1000; удалил — вернулся; операция с чужим account_id даёт 404 |
| 5 | 1. Базовые CRUD Финансов | Контрагенты и Документы: CRUD | Бэкенд | 🟡 Средняя | 1-2ч | Сервер хранит юрлиц/ИП/физлиц и документы (акты; счета; договоры) со статусами | Две вкладки Финансов сейчас полностью нарисованные | новые back/routers/finances/counterparties.py и documents.py; подключить в back/routers/finances/router.py | Контрагенты: GET/POST/PATCH/DELETE; balance и deals_count считать на лету из операций GROUP BY counterparty_id. Документы: GET/POST/PATCH статуса (draft/pending/signed); DELETE; файл в MVP опционален — паттерн загрузки есть в back/routers/studio/media.py | Контрагент создаётся; его deals_count растёт после привязанной операции; документ меняет статус и переживает перезапуск |
| 6 | 1. Базовые CRUD Финансов | Цели: CRUD с авто-прогрессом | Бэкенд | 🟡 Средняя | 1-2ч | Финансовые цели студии; прогресс либо вводится руками либо сам считается из операций | Вкладка «Цели» по ТЗ имеет авто/ручной режим (tracking_mode уже в модели) | новый back/routers/finances/goals.py; back/schemas/finances/goals.py; подключить в finances/router.py | GET/POST/PATCH/DELETE /finances/goals; при tracking_mode=auto current_amount на лету = SUM доходных операций категории цели от created_at до deadline; при manual — брать из PATCH | Авто-цель категории «Абонементы» растёт после дохода этой категории; ручная цель меняется только через PATCH |
| 7 | 1. Базовые CRUD Финансов | Зарплаты: расчёт и выплата | Бэкенд | 🔴 Сложная | 1-2ч | Посчитать зарплату тренера из его занятий и ставки; выплата помечает расчёт и создаёт расходную операцию | Вкладка «Зарплаты» плюс замыкание денег: выплата сама попадает в расходы и отчёты | back/routers/finances/salary.py | GET /finances/salaries?period_start&period_end — по каждому тренеру sessions_count = COUNT занятий (Lesson по teacher_id за период; status != cancelled) и amount по User.rate/rate_type; POST /finances/salaries/{user_id}/pay — создать SalaryPayment со снапшотом ставки + расходную Operation категории «Зарплата» | Расчёт совпадает с числом занятий тренера в Журнале; после выплаты появились SalaryPayment со status=paid и расходная операция на ту же сумму |
| 8 | 2. Агрегация Аналитики | Сводка периода с трендами | Бэкенд | 🔴 Сложная | 1-2ч | Один эндпоинт отдаёт выручку; расходы; прибыль; средний чек; активных клиентов; записи и удержание за период плюс сравнение с прошлым периодом | Сердце эпика: питает 4 метрики Дашборда; Отчёты и вкладку «Отчёты» Финансов | back/routers/analytics/reports.py | GET /analytics/summary?date_from&date_to: SUM операций in/out; avg_check = выручка/число доходных операций; bookings = COUNT активных Reservation за период; active_clients = COUNT клиентов с визитом; retention = доля клиентов прошлого периода вернувшихся в текущем; посещаемость и визиты — по Reservation.status=attended (отметка посещения — Эпик 2 задача 18); блок trends — те же цифры за предыдущий период той же длины и % изменения; только SQLAlchemy SUM/COUNT/GROUP BY — никаких снапшотов | Swagger отдаёт сводку за месяц; цифры сходятся с ручным подсчётом по таблицам; trends показывает знак изменения |
| 9 | 2. Агрегация Аналитики | Ряды для графиков | Бэкенд | 🟡 Средняя | 1-2ч | Отдать точки график-ряда (выручка/записи/клиенты по дням-неделям-месяцам) | График Дашборда; графики Отчётов и линия доход/расход Финансов — один эндпоинт | back/routers/analytics/reports.py | GET /analytics/series?metric=revenue/expenses/bookings/new_clients&group=day/week/month&date_from&date_to через func.date_trunc GROUP BY; ответ список {period; value} | За месяц с group=day приходит ~30 точек; сумма точек revenue равна выручке из /summary за тот же период |
| 10 | 2. Агрегация Аналитики | Срезы по тренерам и услугам | Бэкенд | 🟡 Средняя | 1-2ч | Таблицы «по тренерам» (занятия; выручка) и «по услугам» (выручка; доля) за период | Вкладки Отчётов «По тренерам» и «По услугам» плюс сводные виджеты Дашборда | back/routers/analytics/reports.py | GET /analytics/trainers?date_from&date_to — по каждому тренеру COUNT занятий; выручка из операций по trainer_id; GET /analytics/services — выручка по category/service операций; доля в % от общей | Оба эндпоинта отдают реальные строки; сумма выручки по тренерам не превышает общую из /summary |
| 11 | 2. Агрегация Аналитики | Задачи на сегодня и лента событий | Бэкенд | 🟡 Средняя | 1-2ч | Простой CRUD задач владельца плюс чтение ленты «что произошло в студии» с автозаписью событий | Два виджета Дашборда; лента по ROADMAP наполняется каждым эпиком | новый back/routers/analytics/tasks.py; back/routers/analytics/reports.py; подключить в analytics/router.py; точки записи в back/routers/finances/operations.py и back/routers/clients/profiles.py | CRUD /analytics/tasks под StudioTask (совпасть с analyticsApi: getTasks/createTask/updateTask/deleteTask); GET /analytics/activity (ActivityLog; limit; DESC); хелпер log_activity() и вызовы из создания операции; создания клиента; записи на занятие | Задача создаётся/выполняется/удаляется через Swagger; после создания клиента и операции в /activity появились две записи |
| 12 | 3. Фронт Финансов | Счета и Операции на реальном API | Фронтенд | 🔴 Сложная | 1-2ч | Две главные вкладки Финансов показывают реальные данные; операции грузятся порциями с кнопкой «Загрузить ещё» | Ядро раздела Финансы перестаёт быть декорацией | front/src/pages/dashboard/Finances/components/tabs/AccountsTab.tsx; OperationsTab.tsx; Finances/constants.ts; Finances/types.ts; front/src/api/finances/finances.api.ts и finances.types.ts | Дополнить financesApi: updateAccount; deleteOperation; фильтры и offset/limit в getOperations с типом Page; выпилить ACCOUNTS_DATA и OPERATIONS_DATA из constants.ts; типы вкладок переписать под api-типы; производные поля (client_name; цвета) — адаптерами по CLAUDE.md §8; фильтры вкладки — серверные параметры; «Загрузить ещё» по total | Создал операцию в UI — баланс счёта обновился; F5 ничего не сбрасывает; «Загрузить ещё» дотягивает следующую порцию |
| 13 | 3. Фронт Финансов | Контрагенты и Документы на API | Фронтенд | 🟡 Средняя | 1-2ч | Вкладки контрагентов и документов работают с сервером вместо моков | Ещё две вкладки Финансов становятся настоящими | front/src/pages/dashboard/Finances/components/tabs/CounterpartiesTab.tsx; DocumentsTab.tsx; front/src/api/finances/finances.api.ts | Дополнить financesApi: updateCounterparty; deleteCounterparty; getDocuments; createDocument; updateDocument; deleteDocument; убрать COUNTERPARTIES_DATA и DOCUMENTS_DATA; подключить модалки создания/правки к API | Контрагент и документ создаются/правятся/удаляются из UI и переживают F5; deals_count реальный |
| 14 | 3. Фронт Финансов | Цели и Зарплаты на API | Фронтенд | 🟡 Средняя | 1-2ч | Вкладка целей показывает реальный прогресс; вкладка зарплат — реальные расчёты по тренерам с кнопкой «Выплатить» | Замыкает все MVP-вкладки Финансов (кроме отложенных онлайн-платежей) | front/src/pages/dashboard/Finances/components/tabs/GoalsTab.tsx; SalariesTab.tsx; front/src/api/finances/finances.api.ts | Дополнить financesApi: createGoal; updateGoal; deleteGoal; paySalary; убрать GOALS_DATA и SALARIES_DATA; «Выплатить» вызывает POST pay и обновляет статус | Прогресс авто-цели растёт после операции; «Выплатить» помечает строку и создаёт видимую в Операциях расходную запись |
| 15 | 4. Дашборд и Отчёты | Дашборд на реальные агрегаты | Фронтенд | 🔴 Сложная | 1-2ч | Все виджеты Обзора (4 метрики с трендами; график; лента; задачи; сводки) переходят с моков на эндпоинты аналитики | Первый экран продукта перестаёт врать; ноль дублирования логики — только готовые эндпоинты | front/src/pages/dashboard/Overview/Overview.tsx; constants.ts; hooks/useDashboardChart.ts; components/widgets/MetricsRow.tsx; AnalyticsChart.tsx; TodayTasksWidget.tsx; RecentEventsBoard.tsx; SummaryWidgets.tsx | Метрики и тренды ← /analytics/summary за месяц; график ← /analytics/series; лента ← /analytics/activity; виджет задач ← CRUD /analytics/tasks (analyticsApi уже типизирован — только подключить); сводки ← /analytics/trainers и /analytics/services; выпилить METRICS; chartData; RECENT_EVENTS; TASKS из constants.ts; состояния загрузки | Открыл Дашборд — цифры совпадают с Финансами и Журналом; добавил задачу — она в базе; лента показывает реальные события |
| 16 | 4. Дашборд и Отчёты | Отчёты на агрегаты и честный экспорт | Фронтенд | 🔴 Сложная | 1-2ч | Страница Отчётов и вкладка «Отчёты» Финансов берут цифры из аналитики; экспорт CSV выгружает реальные данные | Закрывает раздел Отчёты; экспорт перестаёт выгружать выдумку | front/src/pages/dashboard/Reports/hooks/useReportData.ts; useDateRange.ts; components/tabs/TabOsnovnye.tsx; TabTrenery.tsx; TabUslugi.tsx; TabProdazhi.tsx; hooks/useExport.ts; front/src/pages/dashboard/Finances/components/tabs/ReportsTab.tsx | Заменить множительную логику useReportData реальными вызовами analyticsApi (summary; series; trainers; services) по выбранному периоду; в exportCSV передавать реальные строки таблиц; ReportsTab Финансов перевести на те же вызовы; PDF оставить window.print | Смена периода меняет цифры через реальные запросы (видно в Network); скачанный CSV содержит данные из базы; вкладка Отчёты Финансов совпадает со страницей Отчётов |
| 17 | 4. Дашборд и Отчёты | Бейджи меню на реальные данные | Фронтенд | 🟢 Простая | 1ч | Захардкоженные счётчики в боковом меню (142 клиента; 3 уведомления) заменить реальными цифрами | Сквозная задача ROADMAP; выдуманные цифры в меню видны на каждой странице продукта | front/src/layouts/DashboardLayout.tsx (строки с nav-badge) | Счётчик клиентов ← total списка клиентов (контракт Эпика 1); счётчик записей/уведомлений ← /analytics/summary или скрыть бейдж до появления реального источника; убрать захардкоженные 142 и 3 | Бейджи совпадают с данными в базе; после добавления клиента счётчик вырос |

---

## Порядок работы (что за чем)

Строго по фазам: **1 → 2 → 3 → 4**.
- Внутри фазы 1: 1 → 2 → 3 → 4, затем 5–7 в любом порядке.
- Фаза 3 (фронт Финансов) стартует после задач 2–7; фаза 4 (Дашборд и Отчёты) — после 8–11; задача 17 (бейджи) — в любой момент фазы 4.
- Задача 7 (зарплаты) и части задач 8/10 (посещаемость; записи) требуют работающего Эпика 2 — без занятий эти цифры будут нулевыми, но эндпоинты не сломаются.

## Что специально НЕ входит в Эпик 3

- **Вкладки «Онлайн-платежи» и «Методы оплаты»** — остаются на моках: каналы приёма — Эпик 4, реальные платежи — Эпик 5.
- **`DailyMetricSnapshot`** — не используется; агрегаты живые. Снапшоты вернутся как оптимизация после MVP, если живые запросы начнут тормозить.
- **Recharts** — не внедряем; кастомные графики уже работают (зависимость висит в package.json, но это долг на потом).
- **Отзывы (StudioReview) и цели тренеров (TrainerSalesGoal)** — модели есть, UI-потребителя в этом эпике нет.
- **Автосоздание операций из оплат** — в Журнале MVP нет приёма денег, поэтому «события оплаты формируют операции» из ROADMAP в этом эпике сводится к ручным операциям и зарплатным выплатам; автоматические операции появятся с онлайн-оплатами в Эпике 5. Факт «визита» закрывает отметка посещения (Эпик 2, задача 18).

## Как проверить (Verification)

1. **Бэкенд:** Swagger (`http://localhost:8000/docs`) — создать счёт → доход 5000 (категория «Абонементы») → расход 2000 → `/analytics/summary` показывает выручку 5000, расходы 2000, прибыль 3000; `/finances/operations` отдаёт Page с total=2.
2. **Зарплата:** у тренера с занятиями (Эпик 2) `/finances/salaries` считает по ставке; `pay` создаёт расходную операцию — расходы в `/summary` выросли.
3. **Фронт:** `cd front && npm run dev` — создать операцию в UI, **обновить страницу (F5)** — всё на месте; Дашборд показывает те же цифры; добавить/выполнить задачу в виджете; выгрузить CSV из Отчётов и открыть файл. `npm run build && npm run lint` — чисто.
