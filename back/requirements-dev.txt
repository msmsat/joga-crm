# Только для разработки: прогон тестов. Прод-зависимости — в requirements.txt
# (снимок pip freeze боевого окружения, pytest туда не добавляем).
pytest==9.1.1
# Без него pytest молча ПРОПУСКАЕТ `async def`-тесты (tests/test_danger_zone.py).
# Режим включается в pytest.ini: asyncio_mode = auto.
pytest-asyncio==1.4.0
