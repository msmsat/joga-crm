export interface UserAccount {
  id: string;
  name: string;
  email: string;
  role: string;
  active: boolean;
  color: string;
}

export interface UserInfo {
  name: string;
  email: string;
  phone: string;
}
