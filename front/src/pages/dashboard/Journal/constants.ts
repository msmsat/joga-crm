// constants.ts

// Тренеры, залы и занятия приходят из API — см. hooks/useSchedule.ts
export const TIMES = ['07:00','08:00','09:00','10:00','11:00','12:00','13:00','14:00','15:00','16:00','17:00','18:00','19:00','20:00','21:00'];

// Названия месяцев/дней недели по локали — utils.ts (monthName/weekdayShort).
/**
 * HB-22 п.4: колонка «Без зала» в группировке по залам.
 *
 * Индивидуальная запись зала может не иметь вовсе (§6.1: resource без зала
 * разрешён), и при отборе `b.hall === hallName` такие карточки исчезали с
 * экрана — не «показывались в другом месте», а пропадали. Сентинел, а не
 * переведённая подпись: перевод меняется, а совпадение колонки с карточкой
 * ломаться от этого не должно.
 *
 * Печатаемая строка, а НЕ управляющий символ: NUL внутри литерала делает файл
 * бинарным для git (ломается diff, blame и grep), и любой инструмент,
 * нормализующий текст, тихо выкинул бы байт, вернув исчезновение карточек.
 * Названия зала `__no_hall__` в продукте быть не может — этого достаточно.
 */
export const NO_HALL_COLUMN = '__no_hall__';
